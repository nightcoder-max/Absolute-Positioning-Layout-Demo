# Absolute-Positioning-Layout-Demo
This project demonstrates a simple HTML layout using absolute positioning. A container holds four boxes arranged in a grid‑like structure, with each box positioned precisely using top and left properties. It’s a clear example of how absolute positioning works inside a relatively positioned parent element.
